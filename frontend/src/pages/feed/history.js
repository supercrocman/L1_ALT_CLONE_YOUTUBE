import React from "react";
import { Typography } from "@mui/material";

const History = () => {
    return (
        <div>
            <Typography>Historique de visionnage</Typography>
        </div>
    );
};

export default History;
